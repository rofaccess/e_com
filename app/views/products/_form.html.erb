<%= form_for(@product) do |f| %>
  <% if @product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@product.errors.count, "error") %> prohibited this product from being saved:</h2>

      <ul>
      <% @product.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name, class: "label" %><br />
    <div class="control">
      <%= f.text_field :name, class: "input" %>
    </div>
  </div>
  <div class="field">
    <%= f.label :price, class: "label" %><br />
    <div class="control">
      <%= f.text_field :price, class: "input" %>
    </div>
  </div>
  <div class="field">
    <%= f.label :created_by_id, class: "label" %><br />
    <div class="control">
      <%= f.text_field :created_by_id, class: "input" %>
    </div>
  </div>

  <div class="field is-grouped">
    <div class="control">
      <%= link_to '<- Back', products_path, class: "button" %>
    </div>

    <% if @product.id.present? %>
      <div class="control">
        <%= link_to 'Show', @product, class: "button" %>
      </div>
    <% end %>

    <div class="control">
      <%= f.submit :class => 'button is-primary' %>
    </div>
  </div>
<% end %>
